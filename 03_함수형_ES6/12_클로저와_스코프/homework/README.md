# 12. 클로저와 스코프 — 과제

## homework_01.js
`makeAdder(n)` 을 작성하세요. 호출하면 `x` 를 받아 `x + n` 을 반환하는 함수를 돌려줍니다.

## homework_02.js
`makeAccount(initial)` 을 작성하세요.
- `deposit(amount)`, `withdraw(amount)`, `balance()` 를 노출
- 잔액은 외부에서 직접 접근 불가
