# 09. 구조분해와 스프레드 — 과제

## homework_01.js
다음 사용자 객체에서 `name`, `age` 를 추출하고 `email` 은 기본값 `"none"` 으로 받으세요. 결과를 출력하세요.
```
{ id: 7, name: "지수", age: 27 }
```

## homework_02.js
두 객체를 spread 로 병합하는 `merge(a, b)` 를 작성하세요. 같은 키가 있으면 `b` 가 우선합니다.
