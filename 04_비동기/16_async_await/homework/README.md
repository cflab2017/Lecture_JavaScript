# 16단원 과제

## 1. 사용자 + 게시글
가짜 `fetchUser(id)` 와 `fetchPosts(userId)` 를 작성하고, async 함수에서 차례로 호출해 결과를 출력하세요.

## 2. 병렬 요청
3개의 URL 에 해당하는 가짜 요청을 Promise.all 로 동시에 시작하고 시간을 측정하세요.

답안은 `answer/` 디렉터리를 참고하세요.
